# dev-server.py を実行すると生成される Python のバイトコードキャッシュ。
# 成果物ではないのでコミットしない。
__pycache__/
*.pyc

# macOS
.DS_Store

# Node(tools/sns-monitor-instagram.js 等が使う Playwright 等の依存)
# package.json / package-lock.json はコミットするが、node_modules と
# Playwright がダウンロードするブラウザ本体はコミットしない(CIで都度インストールする)。
node_modules/
.cache/

