# OGP画像 デザイン仕様書(コピー・レイアウト指示)

作成: コンテンツ制作部 / 2026-08-28
対象: 開発部(この仕様書をもとに実際の画像・SVG/HTMLを実装)

## 共通ルール

- **サイズ**: 1200×630px(1.91:1)。Facebook/Twitter(X)/LINE等の標準比率。
- **セーフゾーン**: 中央80%(左右10%・上下10%を余白として確保)に主要テキスト・ロゴを収める。
  プラットフォームによって周辺をクロップ/縮小表示することがあるため、**端から60px以内には
  読めないと困る情報(文字・ロゴの一部)を置かない**こと。
- **フォント**: サイト本体と同じ `"Hiragino Sans","Yu Gothic UI",system-ui,sans-serif`。
- **配色はサイトのブランドカラーを踏襲**(index.htmlの:root変数と同じ):
  - フェルトグリーン: `#0f3d2e` / `#14513c`
  - ゴールド: `#d9a441` / `#f0c56b`
  - 大会バナー系(既存)は大会ごとに配色を分けている(WJPT=ゴールド基調、JOPT=ティール、
    NIPPON SERIES=赤、FST=濃紺)。この配色分けは踏襲する。
- 既存の1024×412バナー(WJPT/JOPT/NIPPON SERIES/FST)をそのまま1.91:1に引き伸ばすと、
  アスペクト比が変わり(2.49:1 → 1.91:1)左右が詰まって窮屈になるか、上下に余白ができる。
  そのため各画像とも**要素配置を再構成**する前提の指示にしてある(単純リサイズではない)。

---

## 1. サイト共通用OGP(トップページ・エリアページ・店舗ページで共用)

**用途**: `noImage: true` を指定していない汎用ページ全般、および将来トップページのog:imageを
持たせる場合の既定画像。店舗ページ・エリアページは現状 `noImage: true`(og:image無し)だが、
今後付与する場合はこれを使う想定。

### 見出しテキスト(最大文字)
```
ふくおかポーカーナビ
```
- ロゴ的要素: `♠`(スペードマーク、index.htmlの `.logo .pip` と同じ意匠・ゴールド `#f0c56b`)
  をサイト名の直前に配置。

### サブテキスト
```
福岡のポーカー大会・トーナメントを、ぜんぶ、ここで。
```
- 出典: index.html のヒーロー見出し `福岡の<ポーカー大会・トーナメント>を ぜんぶ、ここで。`
  (`<title>` 実測 tone に合わせた既存コピーをそのまま使う。新規コピーを作らない)。

### 補足テキスト(3点、アイコン+短文の横並びまたは3行)
```
天神・中洲・北九州・久留米・筑豊 etc.　毎日更新
掲載店舗数(自動反映される数値。実装時は data.js VENUES.length を焼き込む)
今日・今週の開催が日付で分かる
```
- 「掲載店舗◯◯店」の数値は固定文言にせず、開発部側のビルド時に data.js から動的に埋め込むこと
  (手打ちすると店舗の増減で古くなる。他の生成物と同じ原則)。

### 配置指示
- **中央**: サイト名+ロゴ(♠アイコン)を画面中央やや上に大きく配置。
- **その下**: サブテキスト(キャッチコピー)。
- **最下部**: 3つの補足テキストを横並びチップ(既存の `.vp-list`/`.evt-areas` のピル型デザイン、
  ゴールド枠・白背景・角丸)で並べる。
- **背景**: フェルトグリーンのグラデーション(`linear-gradient(135deg,#0f3d2e,#14513c)`、
  header/heroと同じ)。左右どちらかにトランプのスート(♠♥♦♣)を薄い透かしで散らす程度の装飾は可。
  ただし**文字の可読性を最優先**し、装飾は背景の10%以下の主張に抑える。
- **避けるもの**: 特定の店舗名・大会名(共通画像のため中立性を保つ)、実在の看板・外観写真の使用
  (READMEの編集方針「当サイトは公式媒体ではない」との整合上、公式感を演出する写真は避け、
  自社作成の抽象的なデザインにする)。

---

## 2. FSTページ用OGP

**元データ**: `img/fst/fst-banner.svg`(1024×412、濃紺グラデーション背景・ダイヤ型エンブレム)
**踏襲する内容**: 開催日 9/19-23、賞金総額 ¥15,000,000(MAIN EVENT Prize Total。実装時は
`FST.events[0].prize` を焼き込む。手打ちしない)、キャッチコピー「究極を、遊べ。怒涛の5日間開催」

### 見出しテキスト
```
FST 5.0
FUKUOKA SUPER TOURNAMENT
```
- 既存SVGのダイヤ型エンブレム(FST文字+FUKUOKA SUPER/TOURNAMENTの2行+「FST 5.0」ピル)は
  **セーフゾーン内に収まるサイズで左寄りに再配置**する(現行は1024×412のx=74〜262に配置されており、
  1200×630化でも中央寄りに保てば端に掛からない)。

### サブテキスト
```
2026.9.19 - 9.23
ホテルニューオータニ博多
Prize Total ¥15,000,000（MAIN EVENT）
```

### 配置指示
- **中央〜右**: 日付(大きく、既存SVGと同じ「9.19 ー 9.23」の帯びた数字スタイル)。
- **その下**: 会場名 + Prize Total のピル型バッジ(既存の `fstPill` グラデーションを踏襲)。
- **左**: ダイヤ型エンブレム(FSTロゴ)。**画像端から最低60px内側**に収める(1024×412版はx=74始まりで
  既にほぼセーフだが、1200×630化で単純拡大すると比率がずれるため、比率再計算のうえ配置し直すこと)。
- **避けるもの**: 個別トーナメントの詳細(未発表のため。tools/gen-event-pages.js のコメント通り
  「個別トーナメントの詳細は未発表」の方針を画像側でも踏襲し、公表済みの2大会概要のみ扱う)。

---

## 3. JOPTページ用OGP

**元データ**: `img/jopt/jopt-banner.jpg`(1024×412、ティール基調、クローバー風ロゴ+日程+Prize帯)
**踏襲する内容**: 結果情報(既にページ本文に掲載済み。**新規調査は行わない**、
`tools/gen-event-pages.js` の `JOPT_RESULT` 定数の値をそのまま使う)

### 見出しテキスト
```
JOPT 2026 Fukuoka #01
結果発表
```
- 「結果発表」は既存ページの本文見出し「結果・優勝者（Main Event）」と整合させる。

### サブテキスト
```
優勝: Koheiさん（エントリー 1,179人）
会場: UNITEDLAB（福岡・大名）
7.30 - 8.2
```
- 優勝者名・エントリー数は `JOPT_RESULT.winner` / `JOPT_RESULT.totalEntries` をそのまま焼き込む
  (手打ちしない。出典は `JOPT_RESULT.sourceUrl` のまま変えない)。
- 会場名は `JOPT.venue` を焼き込む。

### 配置指示
- **中央上**: 「JOPT 2026 Fukuoka #01」+「結果発表」を既存バナーのロゴ位置(左)を保ちつつ、
  右側のテキストブロックに統合する形で再配置。
- **中央**: 優勝者名を最大サイズで強調(既存の「MAIN EVENT ¥15,000,000 GTD」ピルと同じ
  ティール系ピル型バッジに「優勝: Koheiさん」を収める)。
- **下**: 会場名・日程を1行で。
- **左**: 既存のクローバー風ロゴ(JAPAN OPEN POKER TOUR)をそのまま流用可。セーフゾーン内に収める。
- **避けるもの**: 準優勝者以下の詳細、注目トーナメントの個別結果(本文には既にあるが、
  OGP画像は「Main Event優勝者」1点に絞って可読性を優先する)。総エントリー数(全体合算6,045)は
  画像内では省略可(本文で確認できるため)。

---

## 4. 日本シリーズページ用OGP

**元データ**: `img/nippon-series/nippon-series-banner.svg`(1024×412、赤基調、白丸「日本」エンブレム)
**依頼3の文言調整と揃える**(「タイムスケジュール」ではなく中立表記、和文「日本シリーズ」を含める、
**結果情報は追加しない**)

### 見出しテキスト
```
NIPPON SERIES（日本シリーズ）
福岡 2026
```
- tools/gen-event-pages.js の新title `NIPPON SERIES（日本シリーズ）福岡2026 大会情報` と
  表記を統一する。

### サブテキスト
```
2026.8.11 - 8.16
福岡 トヨタホールスカラエスパシオ
全38イベント
```
- 「大会情報」であって「タイムスケジュール」ではない点を画像側でも守る(開催前提の
  「まもなく開催」等の煽り文言は入れない。会期は既に終了しているため)。
- **結果・優勝者情報は入れない**(社長方針・2026-08-28: WJPT・日本シリーズの結果調査・記載は
  需要がないと判断済み。既存SVGのMAIN EVENT Prize表記=5,000,000は「公式発表の賞金規模」であり
  「結果」ではないため、この数値の掲載自体は依頼3の制約に抵触しない。ただし「◯◯さんが優勝」のような
  結果文言は絶対に追加しないこと)。

### 配置指示
- **左**: 既存の白丸エンブレム(「日本」の2文字+NIPPON SERIESワードマーク)をそのまま流用。
  セーフゾーン内に収める。
- **中央〜右**: 日付(既存の「8.11 ー 8.16」の帯びた数字スタイルを踏襲)。
- **その下**: 会場名。
- **最下部**: 「全38イベント」のピル型バッジ(既存の `nsPill` 半透明白デザインを踏襲)。
  既存の「MAIN EVENT Prize 5,000,000」の代わりに「全38イベント」を主役にするか、両方を
  小さめに併記するかは開発部の裁量とするが、**「優勝」「結果」の文言だけは入れないこと**。

---

## 5. WJPT用OGP

**元データ**: `img/wjpt/wjpt-banner.jpg`(1024×412、ゴールド基調、虎の顔ロゴが左端ギリギリまで
配置され、右端に地図装飾(北九州の形)がこちらもギリギリまで配置されている)
**課題**: 現行デザインをそのまま1.91:1に引き伸ばすと、虎の顔(左)・地図装飾(右)の両方が
セーフゾーン外に切れる懸念がある。

### 見出しテキスト
```
WJPT
TOURNAMENT SCHEDULE
```
- 既存表記をそのまま踏襲(WJPTは終了済みイベントのアーカイブページで、依頼3のような文言変更対象
  ではない。念のため：WJPTの結果情報の新規追加はこのプロジェクトの対象外=行わない)。

### サブテキスト
```
7.18 - 7.20
WEST JAPAN POKER TOUR ／ 北九州
```

### 配置指示(要素配置の見直し)
- **虎の顔ロゴ**: 現行は左端で顔の輪郭(耳・あご)が画像端で切れている構図。1.91:1化にあたっては
  **顔全体がセーフゾーン内(左端から最低60px内側)に収まるよう縮小・右方向へ再配置**する。
  遠近感を保つため、完全に中央に置く必要はなく「左寄り」を維持してよいが、輪郭が画像端に
  接触しないこと。
- **地図装飾(金色の北九州形シルエット)**: 現行は右端で輪郭が切れている構図。同様に
  **右端から最低60px内側**に収める。輪郭が切れる現行の見た目が「勢いのある演出」として
  意図されている場合でも、OGPは様々なアスペクト比でクロップされるため、**この画像に限っては
  切れない配置を優先**する(通常のサイト内バナー表示=1024×412版は現行のまま変更不要。
  OGP専用に新規カンバスで再構成する)。
- **中央**: 「WJPT」の大型ロゴ(クラウン装飾込み)+「TOURNAMENT SCHEDULE」+日程を中央に配置。
  縦方向の余白(630pxは412pxよりゆとりがある)を活かし、日程行の下にもう1行分の余白を取って
  窮屈にならないようにする。
- **背景**: 既存のダークネイビー+ゴールドの光条演出を踏襲。

---

## 実装時の共通チェックリスト(開発部向け)

1. 1200×630px、セーフゾーン(中央80%=上下左右10%)内に主要テキスト・ロゴが収まっているか。
2. 動的に変わる値(掲載店舗数・Prize金額・優勝者名など)は**手打ちせず、対応するデータソース
  (data.js / big-events.js / jopt-data.js / tools/gen-event-pages.js の JOPT_RESULT 定数)から
  焼き込む**こと(README「数値を手打ちしない」原則)。
3. 日本シリーズ用OGPには結果・優勝者の文言を入れない(社長方針・2026-08-28)。
4. 各画像とも `.jpg` 版を用意すること(構造化データのimageフィールドはSVGを受け付けないため。
  既存の `abs(image)` 呼び出し箇所と同じ制約)。
5. 実装後、`tools/gen-event-pages.js` 等の `image` パスをこの新しいOGP画像に差し替える場合は、
  参照箇所が1箇所(big-events.js の banner または各 build*() 内の image 変数)であることを確認し、
  複数箇所に同じパスをコピーしない。
